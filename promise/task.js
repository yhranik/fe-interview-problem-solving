const fetchData = () => {
  return new Promise((resolve) =>
    setTimeout(() => {
      resolve({ data: Math.random() }), 300;
    })
  );
};

const getRandomSet = async (itemInSet = 3) => {
  const result = [];

  // get result from fetchDate

  return result; // [0.34324234]
};

const printResult = async () => {
  // get print output [23213123]
};
